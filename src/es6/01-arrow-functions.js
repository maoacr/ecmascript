//Funcion tradicional
function square(num) {
  return num * num;
}

//Arrow function
const square = (num) => {
  return num * num;
};

// Sintaxis con return implicito de una Arrow function
const square = (num) => num * num;
